{% extends '::Utilisateurs/utilisateursLayout.html.twig' %}
{% block page %}
    <div>
        {#{% if is_granted("IS_AUTHENTICATED_REMEMBERED") %}#}
            {#<div class=" row-fluid">#}
                {#<div class="col-lg-4 col-lg-offset-4 text-center" style="background-color: darkorange;border-radius: 5px">#}
                    {#<h1>{{ 'layout.logged_in_as'|trans({'%username%': app.user.username}, 'FOSUserBundle') }} |</h1>#}
                    {#<span><a href="{{ path('fos_user_security_logout') }}">#}
                        {#{{ 'layout.logout'|trans({}, 'FOSUserBundle') }}#}
                    {#</a></span>|#}
                    {#<span><a href="{{ path('fos_user_profile_show') }}">Profil</a></span>#}
                {#</div>#}
            {#</div>#}
        {#{% else %}#}
            {#<a href="{{ path('fos_user_security_login') }}">{{ 'layout.login'|trans({}, 'FOSUserBundle') }}</a>#}
        {#{% endif %}#}
    </div>
    {% if app.request.hasPreviousSession %}
        {% for type, messages in app.session.flashbag.all() %}
            {% for message in messages %}
                <div class="flash-{{ type }}">
                    {{ message }}
                </div>
            {% endfor %}
        {% endfor %}
    {% endif %}
    <div>
        {% block fos_user_content %}
        {% endblock fos_user_content %}
    </div>
{% endblock %}
