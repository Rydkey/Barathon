{% extends 'Utilisateurs/utilisateursLayout.html.twig' %}
{% block page %}
{% if is_granted('ROLE_PROP') and app.user.id == event.barId.userId.id %}

    <h1>Modification de l'événement {{ event.libelleevent }}</h1>

        {#{{ form_start(edit_form) }}#}
            {#{{ form_widget(edit_form.) }}#}
            {#<input type="submit" value="Edit" />#}
        {#{{ form_end(edit_form) }}#}

        <div class="row">
            <div class="col-lg-12">
                {{ form_start(edit_form) }}
                    {{ form_widget(edit_form) }}
                <input type="submit" value="Edit" />
                {{ form_end(edit_form) }}
            </div>
        </div>

        <ul>
            <li>
                <a href="{{ path('event_index') }}">Back to the list</a>
            </li>
            <li>
                {{ form_start(delete_form) }}
                    <input type="submit" value="Delete">
                {{ form_end(delete_form) }}
            </li>
        </ul>
{% endif %}
{% endblock %}
